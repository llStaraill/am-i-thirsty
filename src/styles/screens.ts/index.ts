import { StyleSheet } from "react-native";

import { containerWrapper } from "../layout";

export const settingScreenStyling = StyleSheet.create({ containerWrapper });
export const listScreenStyling = StyleSheet.create({ containerWrapper });
export const detailScreenStyling = StyleSheet.create({ containerWrapper });
export const editScreenStyling = StyleSheet.create({ containerWrapper });
export const plantNavigatorStyling = StyleSheet.create({ containerWrapper });
